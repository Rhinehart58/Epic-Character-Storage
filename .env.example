# Optional: override the login-screen changelog URL entirely (forks, staging builds).
# Default is built from update-feed.config.json → GitHub Raw.
# VITE_UPDATE_LOG_URL=https://raw.githubusercontent.com/YOUR_USER/epic-character-storage/main/update-log.json
